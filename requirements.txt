fastapi==0.121.3
uvicorn==0.38.0
requests==2.32.5
pandas==2.3.3
pypdf==6.3.0
openai==2.8.1
# playwright can be omitted because base image includes it,
# but if present, pin to match base image:
playwright==1.56.0
